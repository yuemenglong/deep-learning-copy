# DeepFaceLab SAEHD 模型初始化选项

SAEHD (Self-Attention Extended High-Definition) 是 DeepFaceLab 中最强大的模型之一，它提供了众多可调整的参数，以适应不同场景下的换脸需求。以下是初始化 SAEHD 模型时所有可配置的选项及其详细解释。

## 基本配置选项

### 分辨率 (Resolution)
- **值范围**：64-640，将自动调整为 16 的倍数
- **默认值**：128
- **说明**：输出图像的分辨率。更高的分辨率需要更多的 VRAM 和训练时间，但能提供更清晰的结果。如果选择了 '-d' 架构选项，分辨率将自动调整为 32 的倍数。
- **代码实现**：在模型初始化时，resolution 参数通过 `nn.get4Dshape` 方法影响到输入/输出张量的形状。
- **推荐值**：
  - 4GB VRAM：96-128
  - 6GB VRAM：128-160
  - 8GB VRAM：160-192
  - 11GB+ VRAM：192-256
  - 24GB+ VRAM：256-320
  - 对于最终产品可提高到 512-640，但训练时间会显著增加

### 脸部类型 (Face Type)
- **选项**：
  - `h` (Half Face)：半脸，覆盖面部中心区域
  - `mf` (Mid-Full)：中等全脸，比半脸宽 30%
  - `f` (Full Face)：全脸，覆盖整个面部区域
  - `wf` (Whole Face)：完整面部，包括额头
  - `head` (Head)：整个头部，需要 XSeg 分割
- **默认值**：`f` (Full Face)
- **说明**：决定模型处理脸部的区域范围。半脸分辨率更高但覆盖区域更小，Whole Face 和 Head 需要搭配蒙版使用。
- **代码实现**：影响 `FaceType` 枚举值的选择，该值在样本处理和蒙版生成过程中用于确定处理区域。
- **推荐值**：
  - 面部遮挡、侧脸较多：`h` 或 `mf`（提供更高细节）
  - 标准换脸：`f`（平衡细节和覆盖度）
  - 需要换额头、发际线：`wf`（需要更好的蒙版）
  - 完整头部替换：`head`（必须使用 XSeg 进行准确分割）

### 架构 (Architecture)
- **格式**：`{类型}-{选项}`
- **类型选项**：
  - `df` (DeepFake)：保留更多原始身份特征
  - `liae` (Latent Image Autoencoder)：能更好地修复过于不同的脸型
- **架构选项**：
  - `u`：增强相似度
  - `d`：实验性选项，使用相同计算成本下提高两倍分辨率
  - `t`：使用 Transformer 架构
  - `c`：使用 Conv 架构
- **默认值**：`liae-ud`
- **示例**：df, liae, df-d, df-ud, liae-ud 等
- **说明**：不同架构适合不同场景，df 更适合相似脸型，liae 更适合处理差异较大的脸型。
- **代码实现**：直接影响网络结构的选择。在 df 架构中，使用单独的解码器（decoder_src 和 decoder_dst）；在 liae 中，使用共享解码器和不同的中间层（inter_AB 和 inter_B）。
- **推荐值**：
  - 相似脸型且需要保留身份特征：`df`
  - 不同性别或种族的换脸：`liae`
  - 想要更锐利的结果：添加 `-u` 选项
  - 在相同 VRAM 下想要更高分辨率：添加 `-d` 选项（实验性）

### 自动编码器维度 (AutoEncoder Dimensions)
- **值范围**：32-1024
- **默认值**：256
- **说明**：所有面部信息将被压缩到这些维度中。维度越高，可以捕获更多细节，但需要更多 VRAM。如果维度不足，某些特征（如闭眼）可能无法被正确识别。
- **代码实现**：`ae_dims` 参数直接影响中间层的神经元数量，决定了潜在空间的大小。影响 Inter 网络的维度。
- **推荐值**：
  - 4GB VRAM：128-192
  - 6GB VRAM：192-256
  - 8GB VRAM：256-320
  - 11GB+ VRAM：320-512
  - 24GB+ VRAM：512-768
  - 对于面部表情丰富的视频推荐更高的维度

### 编码器维度 (Encoder Dimensions)
- **值范围**：16-256
- **默认值**：64
- **说明**：更多维度有助于识别更多面部特征，获得更清晰的结果，但需要更多 VRAM。
- **代码实现**：`e_dims` 参数控制编码器网络层的通道数，直接影响特征提取能力。
- **推荐值**：
  - 4GB VRAM：42-64
  - 8GB VRAM：64-85
  - 11GB+ VRAM：85-128
  - 对于需要更好质量且有足够 VRAM 的情况，可以设置为 `ae_dims` 的 1/3 到 1/2

### 解码器维度 (Decoder Dimensions)
- **值范围**：16-256
- **默认值**：64
- **说明**：更多维度有助于识别更多面部特征，获得更清晰的结果，但需要更多 VRAM。
- **代码实现**：`d_dims` 参数控制解码器网络层的通道数，直接影响图像重建能力和清晰度。
- **推荐值**：
  - 通常与 `e_dims` 设置相同
  - 对于需要更好的细节重建，可以略高于 `e_dims`
  - 低 VRAM 环境下可设置为 `e_dims` 的 75%

### 解码器蒙版维度 (Decoder Mask Dimensions)
- **值范围**：16-256
- **默认值**：解码器维度的三分之一
- **说明**：典型蒙版维度为解码器维度的三分之一。如果手动裁剪了目标蒙版中的障碍物，可以增加此参数以获得更好的质量。
- **代码实现**：`d_mask_dims` 参数控制蒙版部分网络的通道数，影响面部边缘和蒙版生成的精确度。
- **推荐值**：
  - 标准情况：`d_dims` 的 1/3
  - 复杂背景或需要精确蒙版：`d_dims` 的 1/2
  - 手动编辑过蒙版的数据集：提高至 `d_dims` 的 2/3

## 训练选项

### 蒙版训练 (Masked Training)
- **选项**：是/否
- **默认值**：是
- **说明**：仅适用于 Whole Face 或 Head 类型。蒙版训练将训练区域限制为全脸蒙版或 XSeg 蒙版，使网络能正确训练面部。
- **代码实现**：通过 `masked_training` 控制损失计算过程。当启用时，只有蒙版区域内的像素会被计算损失，使模型更专注于面部区域。
- **推荐值**：
  - Whole Face/Head 面部类型：建议启用
  - 如果使用了 XSeg 精确分割：强烈推荐启用
  - 如果背景干扰较多：必须启用

### 眼睛和嘴巴优先 (Eyes and Mouth Priority)
- **选项**：是/否
- **默认值**：否
- **说明**：有助于修复训练过程中的眼睛问题，如"异形眼"和错误的眼睛方向。同时提高牙齿细节。
- **代码实现**：当启用时，对眼睛和嘴巴区域的损失进行额外加权（×300），代码中通过 `gpu_target_srcm_em` 和额外的损失项实现。
- **推荐值**：
  - 对眼睛和嘴部表情重要的视频：启用
  - 发现眼睛呈现"异形"状或方向错误：启用
  - 需要提高牙齿细节：启用
  - 简单换脸且源目标脸型相似：可不启用

### 均匀偏航分布 (Uniform Yaw Distribution)
- **选项**：是/否
- **默认值**：否
- **说明**：帮助修复由于数据集中侧脸样本数量较少而导致的侧脸模糊问题。
- **代码实现**：通过 `uniform_yaw_distribution` 参数影响样本生成器的采样策略，确保训练数据中各角度的面部均匀分布。
- **推荐值**：
  - 数据集中侧脸较少：启用
  - 视频中有大量侧脸或快速转头动作：启用
  - 数据集已经角度平衡：可不启用

### 模糊蒙版外部 (Blur Out Mask)
- **选项**：是/否
- **默认值**：否
- **说明**：模糊训练样本应用的面部蒙版外部的周围区域。结果是面部周围的背景变得平滑，在换脸后不太明显。需要源和目标数据集中精确的 XSeg 蒙版。
- **代码实现**：通过 `blur_out_mask` 参数控制，使用高斯模糊处理蒙版外区域，使边界过渡更自然。
- **推荐值**：
  - 使用精确 XSeg 蒙版时：建议启用
  - 需要更自然的面部边缘过渡：启用
  - 背景与面部过渡区有明显差异时：启用

### 模型和优化器放在 GPU 上
- **选项**：是/否
- **默认值**：是
- **说明**：当您在单个 GPU 上训练时，默认情况下，模型和优化器权重放在 GPU 上以加速过程。您可以将它们放在 CPU 上以释放额外的 VRAM，从而设置更大的维度。
- **代码实现**：通过 `models_opt_on_gpu` 参数控制，影响 TensorFlow 设备放置。
- **推荐值**：
  - 低 VRAM GPU (4-6GB)：禁用（放在 CPU 上）
  - 8GB+ VRAM：启用（更快的训练速度）
  - 如果遇到 VRAM 不足错误：禁用

### 使用 AdaBelief 优化器
- **选项**：是/否
- **默认值**：是
- **说明**：使用 AdaBelief 优化器。它需要更多 VRAM，但模型的精度和泛化能力更高。
- **代码实现**：影响优化器的选择，在模型中通过 `OptimizerClass = nn.AdaBelief if adabelief else nn.RMSprop` 实现。
- **推荐值**：
  - 需要更高质量结果：启用
  - VRAM 受限：禁用
  - 短期快速训练：可以禁用
  - 长期高质量训练：强烈建议启用

### 学习率 Dropout
- **选项**：
  - `n`：禁用
  - `y`：启用
  - `cpu`：在 CPU 上启用（节省 VRAM，但迭代时间增加 20%）
- **默认值**：`n`
- **说明**：当面部训练足够时，可以启用此选项，以获得额外的清晰度，并减少像素抖动。推荐在禁用随机变形前启用。
- **代码实现**：通过 `lr_dropout` 参数控制，影响学习率余弦衰减和随机 dropout 的应用。当启用时，lr_cos=500，lr_dropout=0.3；否则 lr_cos=0，lr_dropout=1.0。
- **推荐值**：
  - 训练初期（<25K 迭代）：禁用 (`n`)
  - 模型达到稳定状态后：启用 (`y`)
  - VRAM 不足但需要效果：使用 CPU 模式 (`cpu`)
  - 训练顺序建议：先启用学习率 dropout，然后在更高迭代次数时禁用随机变形

### 启用样本随机变形 (Random Warp)
- **选项**：是/否
- **默认值**：是
- **说明**：随机变形对于泛化两个面部的面部表情是必需的。当面部训练足够时，可以禁用它以获得额外的清晰度，并减少像素抖动。
- **代码实现**：通过 `random_warp` 参数控制样本的几何变形，影响训练数据生成。变形通过 `imagelib.gen_warp_params` 方法应用旋转、缩放和平移等变换。
- **推荐值**：
  - 训练初期（<30K 迭代）：启用
  - 面部已训练良好且表情匹配后：禁用
  - 迭代顺序：1. 启用 random_warp > 2. 启用 lr_dropout > 3. 禁用 random_warp > 4. 启用 GAN
  - 最佳实践：在 100K+ 迭代时禁用，以获得最终的清晰度

### 随机色调/饱和度/亮度强度 (Random HSV Power)
- **值范围**：0.0-0.3
- **默认值**：0.0
- **说明**：仅在神经网络输入时应用于源面部集的随机色调/饱和度/亮度强度。稳定换脸过程中的颜色扰动。通过选择源脸部集中最接近的颜色来减少颜色转移的质量。因此，源脸部集必须足够多样化。典型值为 0.05。
- **代码实现**：通过 `random_hsv_power` 参数控制，影响训练样本的颜色变化强度，对抵抗光照变化有帮助。
- **推荐值**：
  - 光照条件一致的视频：0.0-0.03
  - 光照变化明显的视频：0.05-0.1
  - 需要更强颜色适应性：0.1-0.2
  - 源数据集较小但目标场景多变：0.15-0.3

## GAN 相关选项

### GAN 强度 (GAN Power)
- **值范围**：0.0-5.0
- **默认值**：0.0
- **说明**：强制神经网络学习面部的小细节。只有在已充分训练面部（启用学习率 dropout，关闭随机变形）后才启用，并且不要禁用。值越高，产生伪影的可能性越大。典型值为 0.1。
- **代码实现**：通过 `gan_power` 参数控制 GAN 损失的权重，影响生成对抗网络的训练强度。GAN 部分使用 UNetPatchDiscriminator 实现。
- **推荐值**：
  - 训练初期：0.0（禁用）
  - 模型稳定后（>100K 迭代）：0.05-0.1
  - 需要更多细节但接受一些伪影：0.1-0.3
  - 极端情况（注意可能产生明显伪影）：0.5-1.0
  - 最佳实践：先训练基本模型，然后逐渐增加 GAN 强度

### GAN 补丁大小 (GAN Patch Size)
- **值范围**：3-640
- **默认值**：分辨率的八分之一
- **说明**：补丁大小越大，质量越高，但需要更多 VRAM。即使在最低设置下，也可以获得更锐利的边缘。典型值为分辨率/8。
- **代码实现**：通过 `gan_patch_size` 参数设置判别器网络处理的补丁大小，影响 GAN 的感受野。
- **推荐值**：
  - 低 VRAM：分辨率/16 到 分辨率/12
  - 中等 VRAM：分辨率/10 到 分辨率/8
  - 高 VRAM：分辨率/8 到 分辨率/6
  - 超高 VRAM：分辨率/4 或更大（注意可能导致不稳定）

### GAN 维度 (GAN Dimensions)
- **值范围**：4-512
- **默认值**：16
- **说明**：GAN 网络的维度。维度越高，需要更多 VRAM。即使在最低设置下，也可以获得更锐利的边缘。典型值为 16。
- **代码实现**：通过 `gan_dims` 参数控制判别器网络的通道数，影响 GAN 的容量和学习能力。
- **推荐值**：
  - 低 VRAM：8-12
  - 标准设置：16-32
  - 高 VRAM：32-64
  - 超高 VRAM：64-128（但不推荐过高，可能导致不稳定）

## 风格转移选项

### 真实面部强度 (True Face Power)
- **值范围**：0.0-1.0
- **默认值**：0.0
- **说明**：实验性选项。使结果面部更像源面部。值越高，区分度越强。典型值为 0.01。仅适用于 df 架构。
- **代码实现**：通过 `true_face_power` 参数控制 CodeDiscriminator 的影响力，该判别器试图区分源代码和目标代码，促使保留更多源身份特征。
- **推荐值**：
  - 一般用途：0.0（禁用）
  - 想要保留更多源身份特征：0.005-0.01
  - 源与目标差异较大时：0.01-0.02
  - 注意：值太高可能导致伪影或训练不稳定

### 面部风格强度 (Face Style Power)
- **值范围**：0.0-100.0
- **默认值**：0.0
- **说明**：学习预测面部的颜色与蒙版内的目标相同。如果要在 Whole Face 模式下使用此选项，必须使用经过训练的 XSeg 蒙版。警告：仅在 10k 迭代后启用，此时预测的面部足够清晰以开始学习风格。从 0.001 值开始并检查历史变化。启用此选项会增加模型崩溃的可能性。
- **代码实现**：通过 `face_style_power` 参数控制样式损失的权重，使用 `nn.style_loss` 函数实现，影响颜色和纹理的转移。
- **推荐值**：
  - 标准情况：0.0
  - 初步尝试：0.001-0.005
  - 中等强度：0.01-0.1
  - 强烈风格转移：0.5-2.0
  - 注意：应在模型稳定后谨慎使用，从小值开始

### 背景风格强度 (Background Style Power)
- **值范围**：0.0-100.0
- **默认值**：0.0
- **说明**：学习预测面部蒙版外部区域与目标相同。如果要在 Whole Face 模式下使用此选项，必须使用经过训练的 XSeg 蒙版。这可以使面部更像目标。启用此选项会增加模型崩溃的可能性。典型值为 2.0。
- **代码实现**：类似于面部风格强度，但作用于蒙版外区域，通过 `bg_style_power` 参数控制。
- **推荐值**：
  - 不关注背景：0.0
  - 轻微背景融合：0.5-1.0
  - 中等背景融合：1.0-2.0
  - 强烈背景融合：2.0-5.0
  - 注意：需要精确的 XSeg 蒙版，否则可能产生不自然的边界

### 源数据集颜色转移模式 (Color Transfer Mode)
- **选项**：
  - `none`：不使用颜色转移
  - `rct`：Reinhard 颜色转移
  - `lct`：线性颜色转移
  - `mkl`：Monge-Kantorovitch 线性颜色转移
  - `idt`：迭代分布转移
  - `sot`：切片优化转移
- **默认值**：`none`
- **说明**：更改源样本的颜色分布，使其接近目标样本。尝试所有模式以找到最佳效果。
- **代码实现**：通过 `ct_mode` 参数控制，在样本处理过程中应用不同的颜色转移算法。
- **推荐值**：
  - 源和目标颜色相似：`none`
  - 源和目标有明显色调差异：先尝试 `rct`
  - 需要保留更多源细节：`lct` 或 `idt`
  - 需要更准确的颜色匹配：`mkl` 或 `sot`
  - 最佳实践：逐一尝试所有模式，观察结果选择最佳

### 启用梯度裁剪 (Gradient Clipping)
- **选项**：是/否
- **默认值**：否
- **说明**：梯度裁剪减少模型崩溃的可能性，但会牺牲训练速度。
- **代码实现**：通过 `clipgrad` 参数控制，当启用时 clipnorm=1.0，否则 clipnorm=0.0，影响优化器的梯度裁剪行为。
- **推荐值**：
  - 标准训练：否
  - 使用高 GAN 强度：是
  - 模型训练不稳定或崩溃：是
  - 使用风格转移选项：建议启用

### 启用预训练模式 (Pretraining Mode)
- **选项**：是/否
- **默认值**：否
- **说明**：使用大量不同面部预训练模型。之后，模型可以用于更快地训练换脸。强制 random_warp=N, random_flips=Y, gan_power=0.0, lr_dropout=N, styles=0.0, uniform_yaw=Y。
- **代码实现**：通过 `pretrain` 参数控制，影响多个训练参数的默认值和样本生成策略。
- **推荐值**：
  - 首次训练：先启用预训练 5-10K 迭代
  - 预训练后：禁用，开始针对特定人物的训练
  - 大型多样化数据集：可以完全在预训练模式下训练
  - 注意：预训练需要指定预训练数据路径

## 其他选项

### 随机源翻转 (Random Source Flip)
- **选项**：是/否
- **默认值**：由用户选择
- **说明**：随机水平翻转源图像，增加训练数据多样性。
- **代码实现**：在样本处理选项中设置 `random_flip` 参数，影响数据增强过程。
- **推荐值**：
  - 标准训练：是
  - 预训练：强制启用
  - 源数据集较小：强烈建议启用
  - 特殊情况（如不对称光照）：可考虑禁用

### 随机目标翻转 (Random Destination Flip)
- **选项**：是/否
- **默认值**：由用户选择
- **说明**：随机水平翻转目标图像，增加训练数据多样性。
- **代码实现**：与源翻转类似，但应用于目标样本。
- **推荐值**：
  - 标准训练：是
  - 目标数据集较小：强烈建议启用
  - 特殊情况（如不对称光照或特征）：可考虑禁用

### 批量大小 (Batch Size)
- **默认值**：基于可用 VRAM 自动建议
  - 4GB+ VRAM：建议批量大小为 8
  - 小于 4GB VRAM：建议批量大小为 4
- **说明**：每次迭代处理的样本数量。批量大小越大，训练越稳定，但需要更多 VRAM。
- **代码实现**：通过 `batch_size` 参数控制，影响每次迭代的样本数和内存使用。多 GPU 训练时会自动调整为 `gpu_count*bs_per_gpu`。
- **推荐值**：
  - 4GB VRAM：4-6
  - 6GB VRAM：6-8
  - 8GB VRAM：8-12
  - 11GB+ VRAM：12-16
  - 24GB+ VRAM：16-32
  - 注意：更大的批量大小可提供更稳定的训练，但增加内存需求 